#!/usr/local/bin/haserl
Content-Type: application/json

<%

user="$GET_user$POST_user"
password="$GET_password$POST_password"

[[ "$user" == "" || "$password" == "" ]] && { echo 'user or password empty'; exit 1; }

./tools/auth "$user" "$password" || exit 1

echo '{"status":"success"}'
exit 0

%>
